selection[0] = max(selection[0],0)
selection[0] = min(selection[0],2)

selection[1] = max(selection[1],0)
selection[1] = min(selection[1],2)

selection[2] = max(selection[2],0)
selection[2] = min(selection[2],1)